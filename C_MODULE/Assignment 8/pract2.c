//area of circle
#include<stdio.h>
void calculateAreaOfCircle(float r);
void main()
{
	float r,a;
	printf("The area of the circle with radius %.2f is: %.2f\n",r,a);
	calculateAreaOfCircle(float&r,float&a);
	
}
calculateAreaOfCircle(float r,float a);
{
	float r,a;
	printf("Enter the redius of the circle:");
	scanf("%f",&r);
}
 